%h1 Instagram [@#{instagram_user.username}] (bez geotagu)

%table
  - @log.each do |line|
    %tr
      = (line.map{|s| "<td>#{s}</td>".html_safe} * "\n".html_safe).html_safe

%p= link_to 'Zpět na mapu', :root